package test

import example.listeners.TwitterListener
import example.stores.TweetStore
import org.specs2._

class SamplerTest extends mutable.Specification {
  /*
  "a Sampler" should {

    "print a result to the console at the given sample rate" in {
      //TODO: Test a scheduled service that leaves no trace (i.e. console)? How?
      
      val filter = Some("money")
      val timeLimit = Some(12000)
      val sampleRate = 6000
      val cat1 = List("S.F.","San Francisco")
      val cat2 = List("L.A.","Los Angeles")
      val query = "save"

      TwitterListener.listen(timeLimit, filter, sampleRate, cat1, cat2, query)

    }

  }
  */
}
